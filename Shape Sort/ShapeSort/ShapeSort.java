package ShapeSort;

public enum ShapeT {circle, rectangle, square, triangle};
public enum ColorT {blue, green, purple, red, yellow};

public class Shape implements Comparable
{
	@Override
	public int compareTo(Object o) {
		// TODO Auto-generated method stub
		return 0;
	}

}